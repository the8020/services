version = 1
command = "services.request"
program = "request"
summary = "Send a request through a service"
usage = "<service-id> <method> <relative-path> [options]"
mutates_state = false
restart_behavior = "none"
[[examples]]
command = "services.request the8020/demo/variables GET /health"
